"""
Auth Service (Legacy - functions moved inline to routes)
This module is kept for reference but auth operations are now handled in routes/auth.py
"""

# Note: With WowSQL migration, the auth service functions have been moved
# directly into the routes/auth.py file for simplicity.
# This file can be removed, but is kept to maintain import compatibility.

pass
